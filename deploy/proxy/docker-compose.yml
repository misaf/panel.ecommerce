# /opt/apps/proxy/docker-compose.yml
# The ONLY container on the host that publishes 80/443. Everything else is
# discovered over the shared `traefik-public` network.
#
# One-time host setup:
#   docker network create traefik-public
#   touch proxy/traefik/acme/acme.json && chmod 600 proxy/traefik/acme/acme.json
name: proxy

services:
  traefik:
    image: traefik:v3.7
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    ports:
      - "80:80"
      - "443:443"
      # - "443:443/udp"   # enable with HTTP/3
    environment:
      # DNS-01 provider credential (Cloudflare shown). Used only by the
      # letsencrypt-dns resolver for the *.admin.<base> wildcard. Leave unset in
      # local dev. Keep this in the .env, never commit the real token.
      CF_DNS_API_TOKEN: "${CF_DNS_API_TOKEN:-}"
    volumes:
      # Read-only Docker socket for auto-discovery. Read-only is important:
      # write access to the socket is root on the host.
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./traefik/traefik.yml:/etc/traefik/traefik.yml:ro
      - ./traefik/dynamic:/etc/traefik/dynamic:ro
      - ./traefik/acme:/acme            # acme.json (prod, chmod 600)
      - ./traefik/certs:/certs:ro       # mkcert certs (local dev)
    networks:
      - traefik-public
    labels:
      - "traefik.enable=true"
      # Dashboard: https://traefik.${BASE_DOMAIN}, TLS + basic auth.
      - "traefik.http.routers.dashboard.rule=Host(`traefik.${BASE_DOMAIN}`)"
      - "traefik.http.routers.dashboard.entrypoints=websecure"
      - "traefik.http.routers.dashboard.service=api@internal"
      - "traefik.http.routers.dashboard.tls=true"
      - "traefik.http.routers.dashboard.tls.certresolver=${CERT_RESOLVER:-}"
      - "traefik.http.routers.dashboard.middlewares=dashboard-auth@file,security-headers@file"
    healthcheck:
      test: ["CMD", "traefik", "healthcheck", "--ping"]
      interval: 15s
      timeout: 5s
      retries: 3

# Shared, external, created once by hand. Frontend edge only — no datastore ever
# joins this network.
networks:
  traefik-public:
    external: true
