#!/usr/bin/env bash
#
# Thin wrapper around deploy/stack.sh so you can run `bin/stack <command>`
# from the repo root. All arguments pass straight through.
#
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "$ROOT/deploy/stack.sh" "$@"
